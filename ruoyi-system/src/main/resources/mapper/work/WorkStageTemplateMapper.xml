<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.work.mapper.WorkStageTemplateMapper">

	<resultMap type="WorkStageTemplate" id="WorkStageTemplateResult">
		<id     property="templateId"        column="template_id"       />
		<result property="templateName"      column="template_name"     />
		<result property="deptId"            column="dept_id"           />
		<result property="stagesConfig"      column="stages_config"     />
		<result property="isDefault"         column="is_default"        />
		<result property="version"           column="version"           />
		<result property="usageCount"        column="usage_count"       />
		<result property="status"            column="status"            />
		<result property="createBy"          column="create_by"         />
		<result property="createTime"        column="create_time"       />
		<result property="updateBy"          column="update_by"         />
		<result property="updateTime"        column="update_time"       />
	</resultMap>
	
	<sql id="selectStageTemplateVo">
        select template_id, template_name, dept_id, stages_config, is_default, 
               version, usage_count, status, create_by, create_time, update_by, update_time 
		from work_stage_template
    </sql>
	
	<select id="selectStageTemplateList" parameterType="WorkStageTemplate" resultMap="WorkStageTemplateResult">
	    <include refid="selectStageTemplateVo"/>
		<where>
			<if test="delFlag == null or delFlag == '' or delFlag == '0'">
				AND del_flag = '0'
			</if>
			<if test="templateName != null and templateName != ''">
				AND template_name like concat('%', #{templateName}, '%')
			</if>
			<if test="deptId != null">
				AND (dept_id = #{deptId} or dept_id is null)
			</if>
			<if test="isDefault != null and isDefault != ''">
				AND is_default = #{isDefault}
			</if>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
		</where>
		order by is_default desc, usage_count desc, create_time desc
	</select>
	
	<select id="selectStageTemplateAll" resultMap="WorkStageTemplateResult">
		<include refid="selectStageTemplateVo"/>
		where del_flag = '0'
		order by is_default desc, usage_count desc, create_time desc
	</select>
	
	<select id="selectStageTemplateById" parameterType="Long" resultMap="WorkStageTemplateResult">
		<include refid="selectStageTemplateVo"/>
		where template_id = #{templateId} and del_flag = '0'
	</select>
	
	<select id="selectTemplatesByDeptId" parameterType="Long" resultMap="WorkStageTemplateResult">
		<include refid="selectStageTemplateVo"/>
		where (dept_id = #{deptId} or dept_id is null) and del_flag = '0' and status = '0'
		order by is_default desc, usage_count desc
	</select>
	
	<select id="selectDefaultTemplate" resultMap="WorkStageTemplateResult">
		<include refid="selectStageTemplateVo"/>
		where is_default = '1' and status = '0' and del_flag = '0' limit 1
	</select>
	
	<select id="checkTemplateNameUnique" parameterType="String" resultMap="WorkStageTemplateResult">
		<include refid="selectStageTemplateVo"/>
		where template_name = #{templateName} and del_flag = '0' limit 1
	</select>
	
 	<delete id="deleteStageTemplateByIds" parameterType="Long">
 		update work_stage_template set del_flag = '2' where template_id in
 		<foreach collection="array" item="templateId" open="(" separator="," close=")">
 			#{templateId}
        </foreach>
 	</delete>
 	
 	<update id="updateStageTemplate" parameterType="WorkStageTemplate">
 		update work_stage_template
 		<set>
 			<if test="templateName != null and templateName != ''">template_name = #{templateName},</if>
 			<if test="deptId != null">dept_id = #{deptId},</if>
 			<if test="stagesConfig != null">stages_config = #{stagesConfig},</if>
 			<if test="isDefault != null and isDefault != ''">is_default = #{isDefault},</if>
 			<if test="version != null">version = #{version},</if>
 			<if test="usageCount != null">usage_count = #{usageCount},</if>
 			<if test="status != null and status != ''">status = #{status},</if>
 			<if test="remark != null">remark = #{remark},</if>
 			<if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
 			update_time = sysdate()
 		</set>
 		where template_id = #{templateId}
	</update>
	
	<update id="incrementUsageCount">
		update work_stage_template 
		set usage_count = usage_count + 1, update_time = sysdate()
		where template_id = #{templateId}
	</update>
 	
 	<insert id="insertStageTemplate" parameterType="WorkStageTemplate" useGeneratedKeys="true" keyProperty="templateId">
 		insert into work_stage_template(
 			<if test="templateId != null and templateId != 0">template_id,</if>
 			<if test="templateName != null and templateName != ''">template_name,</if>
 			<if test="deptId != null">dept_id,</if>
 			<if test="stagesConfig != null">stages_config,</if>
 			<if test="isDefault != null and isDefault != ''">is_default,</if>
 			<if test="version != null">version,</if>
 			<if test="usageCount != null">usage_count,</if>
 			<if test="status != null and status != ''">status,</if>
 			<if test="remark != null and remark != ''">remark,</if>
 			<if test="createBy != null and createBy != ''">create_by,</if>
 			create_time
 		)values(
 			<if test="templateId != null and templateId != 0">#{templateId},</if>
 			<if test="templateName != null and templateName != ''">#{templateName},</if>
 			<if test="deptId != null">#{deptId},</if>
 			<if test="stagesConfig != null">#{stagesConfig},</if>
 			<if test="isDefault != null and isDefault != ''">#{isDefault},</if>
 			<if test="version != null">#{version},</if>
 			<if test="usageCount != null">#{usageCount},</if>
 			<if test="status != null and status != ''">#{status},</if>
 			<if test="remark != null and remark != ''">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			sysdate()
 		)
	</insert>

</mapper>